## Problem to solve

In Elo `==` is ALWAYS value equality. On all types.

Let's make sure it's actually the case, on all Scalar and Collection types.

## Idea

- Check existing tests for coverage
- Add missing acceptance tests, if any
