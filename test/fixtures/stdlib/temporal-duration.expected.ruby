->(_) { (raise "Assertion failed" unless Date.parse('2024-01-15') + ActiveSupport::Duration.parse('P1D') > Date.parse('2024-01-15'); true) }.call(nil);
->(_) { (raise "Assertion failed" unless Date.parse('2024-01-15') + ActiveSupport::Duration.parse('P1D') < Date.parse('2024-01-17'); true) }.call(nil);
->(_) { (raise "Assertion failed" unless Date.parse('2024-01-15') - ActiveSupport::Duration.parse('P1D') < Date.parse('2024-01-15'); true) }.call(nil);
->(_) { (raise "Assertion failed" unless Date.parse('2024-01-15') - ActiveSupport::Duration.parse('P1D') > Date.parse('2024-01-13'); true) }.call(nil);
->(_) { (raise "Assertion failed" unless Date.parse('2024-01-01') + ActiveSupport::Duration.parse('P1D') * 2 == Date.parse('2024-01-03'); true) }.call(nil);
->(_) { (raise "Assertion failed" unless Date.parse('2024-01-01') + 2 * ActiveSupport::Duration.parse('P1D') == Date.parse('2024-01-03'); true) }.call(nil);
->(_) { (raise "Assertion failed" unless Date.parse('2024-01-01') + ActiveSupport::Duration.parse('P2D') / 2 == Date.parse('2024-01-02'); true) }.call(nil);
->(_) { (raise "Assertion failed" unless (->(v) { case v when NilClass; 'Null' when ActiveSupport::Duration; 'Duration' when Date, DateTime, Time; 'DateTime' when Integer; 'Int' when Float; 'Float' when TrueClass, FalseClass; 'Bool' when String; 'String' when Proc; 'Function' when Array; 'List' else 'Tuple' end }).call(-1 * ActiveSupport::Duration.parse('P1D')) == "Duration"; true) }.call(nil);
->(_) { (raise "Assertion failed" unless Date.parse('2024-01-03') + -1 * ActiveSupport::Duration.parse('P1D') == Date.parse('2024-01-02'); true) }.call(nil);
->(_) { (raise "Assertion failed" unless (->(v) { case v when NilClass; 'Null' when ActiveSupport::Duration; 'Duration' when Date, DateTime, Time; 'DateTime' when Integer; 'Int' when Float; 'Float' when TrueClass, FalseClass; 'Bool' when String; 'String' when Proc; 'Function' when Array; 'List' else 'Tuple' end }).call((Date.parse('2024-01-15') - Date.parse('2024-01-10')).to_i.days) == "Duration"; true) }.call(nil);
->(_) { (raise "Assertion failed" unless (Date.parse('2024-01-15') - Date.parse('2024-01-10')).to_i.days == ActiveSupport::Duration.parse('P5D'); true) }.call(nil);
