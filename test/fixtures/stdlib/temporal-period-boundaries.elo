assert(SOD <= NOW)
assert(EOD >= NOW)
assert(SOD < EOD)
assert(SOW <= TODAY)
assert(EOW >= TODAY)
assert(SOW < EOW)
assert(SOM <= TODAY)
assert(EOM >= TODAY)
assert(SOM < EOM)
assert(SOQ <= TODAY)
assert(EOQ >= TODAY)
assert(SOQ < EOQ)
assert(SOY <= TODAY)
assert(EOY >= TODAY)
assert(SOY < EOY)
